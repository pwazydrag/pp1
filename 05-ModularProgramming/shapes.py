def drawSquare(x,y,n):
    import turtle
    #turtle.penup()
    #turtle.setpos(x,y)
  #  turtle.setx(x)
   # turtle.sety(y)
    #turtle.pendown()
    for o in range (0,4):
        turtle.forward(n)
        turtle.right(90)
    #print(turtle.position())
    turtle.forward(n)
    #turtle.done()